OPENAI_API_KEY=sk-replace-me

# Optional: override the models defined in prompts/v1.yaml at runtime.
ROUTER_MODEL=
PLANNER_MODEL=
AGENT_MODEL=
JUDGE_MODEL=

# Paths (defaults are fine if you run from the repo root).
TRIAGE_PROMPTS_DIR=prompts
TRIAGE_RUNBOOK_DIR=data/runbooks
